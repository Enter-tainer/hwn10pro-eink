[package]
name = "hweink-demo"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "hweink-demo"
path = "src/main.rs"

[dependencies]
hweink = { path = "../libhweink" }
libc = { version = "0.2", default-features = false }
